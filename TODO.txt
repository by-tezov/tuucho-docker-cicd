- script pour gradle setup download and create archive (example miss 9.0.0 wrapper)

# TODO docker
    - source all variable in single file to make it easier to setup somewhere else instead of fetching entrypoint by entrypoint
    - env variable system should come from docker env instead of duplicate conf (aka path builder env, +node path), maybe a way to inject them in jenkins ?
    - todo agent-repository for (report, apk, app, ipa, git clone...)

# one day todo
- multiple suite build variant allowed
- emulator android on the fly (+self destruction)
- appium running and stop with ARC logic instead of always start/stop
